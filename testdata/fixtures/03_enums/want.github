::error file=domain/entities.go,line=27,col=2,title=E007 enum labels differ::the labels of public.color and the constants of Color differ%0A%0AColor has no constant for "green"; public.color has no label for ColorBlue ("blue")%0A%0AFix: add the missing constants, or change the PostgreSQL enum so both sides agree
::error file=domain/entities.go,line=29,col=2,title=E006 incompatible Go and PostgreSQL types::Shade is string but public.items.shade is the enum public.color%0A%0Aan enum column maps to a named Go string type whose constants are its labels, so that the two can be reconciled%0A%0AFix: declare a named string type with one constant per label (red, green)
