domain/entities.go:21:2: error: E019: domain.User.Orgs is orm.Many but the foreign key is on public.users
    Go:          domain.User.Orgs
    PostgreSQL:  public.users
    relation:    Orgs
    constraint:  users_org_id_fkey
    reason:      public.users holds one org_id per row, so it can reference at most one domain.Org
    fix:         declare the field as orm.One[Org], or move the foreign key to public.orgs

1 finding: 1 error, 0 warnings
