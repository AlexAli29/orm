::error file=domain/entities.go,line=19,col=2,title=E010 remote-FK to-one relation is not guaranteed unique::domain.Employee.Deputy is orm.One but public.employees(manager_id) is not unique%0A%0Anothing stops two rows from referencing the same one, so a to-one relation would silently pick whichever came back first%0A%0AFix: add a total unique constraint on public.employees(manager_id), or declare the field as orm.Many[Employee]
::error file=domain/entities.go,line=29,col=2,title=E009 relation foreign key is ambiguous::domain.Node.Parent is ambiguous: nodes_origin_fkey, nodes_parent_fkey%0A%0Athe table has more than one foreign key to itself%0A%0AFix: pin the constraint with orm:"fk:nodes_origin_fkey", and add side:local or side:remote if the direction is also ambiguous
