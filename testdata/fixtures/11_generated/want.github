::error file=domain/entities.go,line=5,col=1,title=E002 unmapped NOT NULL column with no default or generated value::public.invoices.domain_note is NOT NULL with no default and is not mapped%0A%0Adomain.Invoice cannot supply a value for it, so no row can be inserted through this entity%0A%0AFix: map the column on domain.Invoice, give it a default, or make it nullable
::error file=domain/entities.go,line=5,col=1,title=E002 unmapped NOT NULL column with no default or generated value::public.invoices.required_note is NOT NULL with no default and is not mapped%0A%0Adomain.Invoice cannot supply a value for it, so no row can be inserted through this entity%0A%0AFix: map the column on domain.Invoice, give it a default, or make it nullable
::warning file=domain/entities.go,line=5,col=1,title=W003 PostgreSQL column exists but is unmapped::column public.invoices.created_at is not mapped%0A%0Athe column has a default, so inserts succeed without it, but its value cannot be read back%0A%0AFix: add a field for created_at to domain.Invoice, or leave it unmapped on purpose and set strict.unmapped_columns: off
::warning file=domain/entities.go,line=5,col=1,title=W003 PostgreSQL column exists but is unmapped::column public.invoices.optional_note is not mapped%0A%0Athe column is nullable, so inserts succeed without it, but its value cannot be read back%0A%0AFix: add a field for optional_note to domain.Invoice, or leave it unmapped on purpose and set strict.unmapped_columns: off
::warning file=domain/entities.go,line=5,col=1,title=W003 PostgreSQL column exists but is unmapped::column public.invoices.seq is not mapped%0A%0Athe column has a default, so inserts succeed without it, but its value cannot be read back%0A%0AFix: add a field for seq to domain.Invoice, or leave it unmapped on purpose and set strict.unmapped_columns: off
