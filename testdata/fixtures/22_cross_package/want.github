::error file=sales/entities.go,line=23,col=2,title=E024 relation target is in another package::sales.Order.Product targets catalog.Product, which is in another package%0A%0Aa relation is loaded through the target's generated descriptors, and those are generated into the target's own package where this one cannot reach them%0A%0AFix: declare sales.Order and catalog.Product in one package, or drop the relation and read the foreign key column directly
